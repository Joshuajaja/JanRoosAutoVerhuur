<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="JanRoosAutoVerhuur.MainPage"
             xmlns:viewmodels="clr-namespace:JanRoosAutoVerhuur.Viewmodel"
             xmlns:views="clr-namespace:JanRoosAutoVerhuur"
             x:DataType="viewmodels:MainViewModel"
             >

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Border StrokeShape="RoundRectangle 10,10,10,10"
                    Margin="0,30,0,0"
                    HorizontalOptions="FillAndExpand"
                    Padding="10,0"
                    Stroke="{StaticResource Tertiary}"
                    StrokeThickness="0"
                    BackgroundColor="{StaticResource Secondary}">
                <FlexLayout
                  HorizontalOptions="FillAndExpand"
                  Wrap="Wrap"
                  Direction="Row"
                  AlignItems="Center"
                  JustifyContent="Center"
                  HeightRequest="40">
                    <HorizontalStackLayout HorizontalOptions="Center">
                        <Label
                        Text="Filters:"
                        Margin="10,0,10,0"
                        FontFamily="OpenSansBold"
                        TextColor="{StaticResource Tertiary}"
                        VerticalTextAlignment="Center"
                        HorizontalTextAlignment="Center"/>

                        <Picker x:Name="FilterPicker1"
                                ItemsSource="{Binding Brands}"
                                BackgroundColor="{StaticResource Secondary}"
                                TextColor="{StaticResource Tertiary}"
                                FontFamily="OpenSansBoldItalic"
                                Margin="0,10"
                                HeightRequest="2"
                                VerticalOptions="Center"
                                HorizontalOptions="Fill">
                        </Picker>
                        <Picker x:Name="FilterPicker2"
                                ItemsSource="{Binding Color}"
                                BackgroundColor="{StaticResource Secondary}"
                                TextColor="{StaticResource Tertiary}"
                                FontFamily="OpenSansBoldItalic"
                                Margin="0,10"
                                HeightRequest="2"
                                VerticalOptions="Center"
                                HorizontalOptions="Fill">
                        </Picker>
                        <Picker x:Name="FilterPicker3"
                                ItemsSource="{Binding Seats}"
                                BackgroundColor="{StaticResource Secondary}"
                                TextColor="{StaticResource Tertiary}"
                                FontFamily="OpenSansBoldItalic"
                                Margin="0,10"
                                HeightRequest="2"
                                VerticalOptions="Center"
                                HorizontalOptions="Fill">
                        </Picker>
                        <Picker x:Name="FilterPicker4"
                                ItemsSource="{Binding Age}"
                                BackgroundColor="{StaticResource Secondary}"
                                TextColor="{StaticResource Tertiary}"
                                FontFamily="OpenSansBoldItalic"
                                Margin="0,10"
                                HeightRequest="2"
                                VerticalOptions="Center"
                                HorizontalOptions="Fill">
                        </Picker>
                        <Picker x:Name="FilterPicker5"
                                ItemsSource="{Binding Type}"
                                BackgroundColor="{StaticResource Secondary}"
                                TextColor="{StaticResource Tertiary}"
                                FontFamily="OpenSansBoldItalic"
                                Margin="0,10"
                                HeightRequest="2"
                                VerticalOptions="Center"
                                HorizontalOptions="Fill">
                        </Picker>
                        <Picker x:Name="FilterPicker6"
                                ItemsSource="{Binding Class}"
                                BackgroundColor="{StaticResource Secondary}"
                                TextColor="{StaticResource Tertiary}"
                                FontFamily="OpenSansBoldItalic"
                                Margin="0,10"
                                HeightRequest="2"
                                VerticalOptions="Center"
                                HorizontalOptions="Fill">
                        </Picker>
                    </HorizontalStackLayout>
                </FlexLayout>
            </Border>
            <Grid RowDefinitions="Auto, *">

                <!-- Scrollable card grid -->
                <CollectionView
     Grid.Row="1"
     ItemsSource="{Binding Cars}"
     Margin="10">

                    <CollectionView.ItemsLayout>
                        <GridItemsLayout
        Orientation="Vertical"
        Span="{Binding CardSpan}" />
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <views:Kaartje />
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                </CollectionView>



            </Grid>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>